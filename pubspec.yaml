name: flutter_simple_provider
description: Simple Provider for Flutter
publish_to: 'none'
version: 1.0.0+2
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  # TODO 0 : library/dependencies
  provider: ^6.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
